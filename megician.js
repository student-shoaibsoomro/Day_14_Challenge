"use strict";
let megician = ['David Blaine', 'David Copperfield', 'Derren Brown'];
function show_megicians(megicians) {
    megicians.forEach(element => {
        console.log(element);
    });
}
show_megicians(megician);
