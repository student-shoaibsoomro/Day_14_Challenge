let megician: string[] = ['David Blaine', 'David Copperfield', 'Derren Brown'];
function show_megicians(megicians: string[]){
    megicians.forEach(element => {
        console.log(element);
    });
}
show_megicians(megician)